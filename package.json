{
  "name": "coin-flipper",
  "private": true,
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix coinflipper-react",
    "server": "npm run dev --prefix backend",
    "client": "npm run dev --prefix coinflipper-react",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
